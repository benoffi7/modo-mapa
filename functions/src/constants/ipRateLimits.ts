/** Max writes per IP per day across all callable functions */
export const MAX_WRITES_PER_IP_PER_DAY = 200;

/** Max anonymous account creations per IP per day */
export const MAX_ANON_CREATES_PER_IP_PER_DAY = 10;

/** Alert threshold: log abuse warning when this many anon creates from same IP */
export const ANON_FLOOD_ALERT_THRESHOLD = 5;
